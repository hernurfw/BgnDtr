<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.naitogami.bangundatar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".persegi.persegi" />
        <activity android:name=".persegiPanjang.PersegiPanjang" />
        <activity android:name=".segitiga.segitiga" />
        <activity android:name=".lingkaran.lingkaran" />
        <activity android:name=".tentang"></activity>
    </application>

</manifest>